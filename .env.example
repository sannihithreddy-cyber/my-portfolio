MONGODB_URI=
PORT=5000
SEED_TOKEN=change-me

# Email (free options)
# Option A: Your own SMTP (e.g., Gmail App Password, Brevo SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Who receives messages (defaults to profile.email if omitted)
MAIL_TO=
# Optional from address and subject
MAIL_FROM=
MAIL_SUBJECT=
